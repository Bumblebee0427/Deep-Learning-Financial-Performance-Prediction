# 九目标回归：锁定配方与预留折复核

所有实验只读取 `train.csv`，使用同一份 `evaluation_framework.py` 中的 rubric sMAPE（两者同为零时误差记零）。每个模型的缺失值填补和类别编码都只在该折训练部分拟合。`Id` 不进入特征。完整 OOF 预测保存为 Parquet，并包含 `row_index` 和 `Id` 以检查逐行对齐。没有生成测试集提交。

## 1. 锁定原配方：seed=2026

原始 seed=42 配方来自 `oof_outputs/target_recipes.csv`，SHA-256 为 `c7546ae4a87d6b561156cd0d050085e3485a12865db604ac1e0a2385640d8a34`。运行前复制到 `locked_cv_2026/target_recipes_frozen.csv`，模型选择、RF/LGBM 比例、滞后值比例和会计恒等式权重均未调整。新折为 `KFold(n_splits=5, shuffle=True, random_state=2026)`。

| 目标 | 原 seed=42 已选 OOF | seed=2026 锁定配方 |
|---|---:|---:|
| 总资产 | 4.335% | 4.271% |
| 总负债 | 9.656% | 9.550% |
| 股东权益 | 10.276% | 10.191% |
| 毛利润 | 23.145% | 23.057% |
| 营业成本 | 18.051% | 18.051% |
| 收入 | 13.805% | 13.760% |
| 营业利润 | 50.590% | 50.522% |
| 营业费用 | 11.045% | 10.992% |
| EBITDA | 50.357% | 50.355% |
| **九目标平均** | **21.2509%** | **21.1944%** |

新折减小 **0.0565 个百分点**。五折平均依次为 **20.9574%、21.3867%、21.3143%、21.2125%、21.1011%**。原配方在另一组未用于选权重的分折（seed=314159）为 **21.2064%**。逐折和逐目标原始结果见 `locked_cv_2026/` 与 `locked_cv_314159/`。

## 2. 九目标 arcsinh 消融

下表在 seed=2026 的完全相同折、基础特征、预处理和 LightGBM 参数下，只改变目标是否以训练折的 `median(abs(y))` 缩放后做 `arcsinh`。表中是**单模型**分数；没有把它们当作最终配方分数。每个目标的 arcsinh 都在 **5/5 折**优于原值目标训练。

| 目标 | 原值目标 | arcsinh 目标 | 改善百分点 |
|---|---:|---:|---:|
| 总资产 | 14.932% | 4.591% | 10.341 |
| 总负债 | 16.576% | 5.367% | 11.208 |
| 股东权益 | 21.338% | 9.292% | 12.046 |
| 毛利润 | 35.908% | 23.397% | 12.511 |
| 营业成本 | 22.614% | 11.606% | 11.007 |
| 收入 | 19.579% | 9.354% | 10.225 |
| 营业利润 | 59.256% | 51.555% | 7.701 |
| 营业费用 | 12.379% | 6.184% | 6.195 |
| EBITDA | 59.695% | 50.557% | 9.138 |
| **九目标平均** | **29.142%** | **19.100%** | **10.041** |

原值组预测与锁定实验中基础 LightGBM 的逐行预测完全相同（最大绝对差为零）。详见 `ablation_cv_2026/target_scores.csv` 和 `fold_scores.csv`。

## 3. 困难目标表示实验

这组使用 seed=2026 的相同五折和目标专属滞后特征。收入比率的还原使用该验证行的**OOF 收入预测**；EBITDA 残差法使用该行的**OOF 营业利润预测**。这些跨目标输入没有使用验证行的真实 Q0 值。

| 目标 | 方法 | sMAPE | 符号准确率 |
|---|---|---:|---:|
| 营业利润 | 直接 arcsinh | 51.778% | 92.620% |
| 营业利润 | 利润率 × OOF 收入 | 51.043% | 91.920% |
| 营业利润 | 符号分类＋arcsinh 金额 | **43.790%** | **92.967%** |
| EBITDA | 直接 arcsinh | 50.355% | 92.831% |
| EBITDA | 利润率 × OOF 收入 | 50.125% | 92.236% |
| EBITDA | 符号分类＋arcsinh 金额 | **42.911%** | **93.317%** |
| EBITDA | OOF 营业利润＋预测残差 | 48.641% | 92.892% |

两个目标的符号＋金额法都在 **5/5 折**优于直接 arcsinh。逐折分数见 `representation_cv_2026/`。

## 4. 小范围 LightGBM 搜索与候选配方

因为九个目标的 arcsinh 消融都获益，九个目标均进入受控搜索。只比较原配置、较小/较大叶子、较低学习率配较多树，以及 `regression_l1` 五种配置；没有搜索会计或滞后权重。搜索在 seed=2026 上选配置，且每个胜出配置均在 **5/5 折**优于原 arcsinh 配置。配置和分数见 `search_cv_2026/development_selection.csv` 及各目标 `*_scores.csv`。

接着按固定目标顺序尝试替换原配方中的直接预测：要求完整九目标平均 sMAPE 至少改善 0.05 个百分点，并且至少 3/5 折改善。每个被选中的替换实际都在 **5/5 折**改善完整配方。会计恒等式的比例与非负截断保持原值。开发折平均分数由 **21.1944%** 到 **16.5413%**；这只是模型选择分数。

| 目标 | 推荐的直接预测 | 最终输出规则 |
|---|---|---|
| 总资产 | arcsinh LightGBM；63 叶、最小叶样本 20 | 直接预测 |
| 总负债 | arcsinh LightGBM；63 叶、最小叶样本 20 | 直接预测与（总资产直接预测−权益直接预测）各占 50% |
| 股东权益 | arcsinh LightGBM；63 叶、最小叶样本 20 | 总资产直接预测−负债直接预测 |
| 毛利润 | 滞后特征＋arcsinh LightGBM；L1 损失 | 直接预测 |
| 营业成本 | arcsinh LightGBM；L1 损失 | 直接预测 |
| 收入 | 原直接模型维持不变 | `max(0, 毛利润直接预测＋营业成本直接预测)`；原直接收入预测不进入最终输出 |
| 营业利润 | 滞后特征＋LightGBM 符号分类和 arcsinh 金额回归 | 直接预测与（毛利润直接预测−营业费用直接预测）各占 50% |
| 营业费用 | arcsinh LightGBM；63 叶、最小叶样本 20 | `max(0, 0.25×直接预测＋0.75×(毛利润直接预测−营业利润直接预测))` |
| EBITDA | 滞后特征＋LightGBM 符号分类和 arcsinh 金额回归 | 直接预测 |

除上表特别写出的配置外，LightGBM 使用 120 棵树、学习率 0.08、`colsample_bytree=0.8`；符号分类以 `P(y>0)≥0.5` 决定符号。所有目标的完整参数可在 `lightgbm_small_search.py` 和 `candidate_recipe_cv.py` 中复现。选择决定固定在 `candidate_cv_314159/selected_substitutions.json`。

## 5. 预留 seed=314159 复核

候选配方在查看 seed=314159 的结果前已固定。该分折重新训练了原配方和选中的所有候选模型；**未在该分折重新选配置或权重**。

| 目标 | 原锁定配方 | 候选配方 | 改善百分点 |
|---|---:|---:|---:|
| 总资产 | 4.274% | **3.340%** | 0.934 |
| 总负债 | 9.486% | **5.522%** | 3.964 |
| 股东权益 | 10.157% | **6.969%** | 3.189 |
| 毛利润 | 23.091% | **20.550%** | 2.541 |
| 营业成本 | 18.051% | **10.147%** | 7.904 |
| 收入 | 13.774% | **8.355%** | 5.420 |
| 营业利润 | 50.602% | **42.666%** | 7.935 |
| 营业费用 | 11.015% | **8.514%** | 2.501 |
| EBITDA | 50.408% | **43.085%** | 7.323 |
| **九目标平均** | **21.2064%** | **16.5719%** | **4.6345** |

| 折 | 原锁定配方 | 候选配方 | 改善百分点 |
|---:|---:|---:|---:|
| 1 | 21.2183% | 16.6151% | 4.6032 |
| 2 | 21.1640% | 16.5043% | 4.6597 |
| 3 | 21.2057% | 16.4828% | 4.7230 |
| 4 | 21.2484% | 16.6768% | 4.5717 |
| 5 | 21.1957% | 16.5807% | 4.6149 |

九个目标与五折的改进方向均一致。这里的“预留”指未用于本轮候选选择的 **CV 随机种子**；它与开发实验使用同一批 100,000 行训练样本，而且更早的 seed=42 选择过程也已见过这些行的标签。因此它是跨分折稳定性检查，**不是独立留出样本或测试集泛化分数**。如果以后需要无偏的模型比较，应先隔离从未参与任何选择的行或获得新标签数据。

另需核实数据字典中的同季度元数据 `totalRevenue` 与 `ebitda` 在真实预测时是否可用；现有特征管线沿用了它们。若这些字段在预测时不可获得，全部结果都应在剔除它们后重新评估。
